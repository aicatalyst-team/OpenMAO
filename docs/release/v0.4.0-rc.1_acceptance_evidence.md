# v0.4.0-rc.1 Acceptance Evidence

This document maps the first real side-effecting provider release to code and test evidence.
It does not replace maintainer release approval or independent review.

**Status:** release candidate
**Last updated:** 2026-05-29
**Runtime:** TypeScript on Node.js 22 LTS

## Summary

v0.4.0 makes one real, side-effecting capability enforceable end to end through an OpenMAO-managed
provider and credential broker: a GitHub issue-comment provider. It validates the enforced-capability
boundary against a genuine network side effect while keeping the default demo deterministic and
credential-free.

## Verification Commands

```bash
make check
rm -rf .openmao && make demo && make demo-approve && rm -rf .openmao
rm -rf .openmao && npm run cli -- worker demo && npm run cli -- approvals approve approval_bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb && rm -rf .openmao
```

Latest verification on 2026-05-29:

- `make check` passed: TypeScript lint, typecheck, 105 Vitest tests across the public `ts/tests`
  suite, and public hygiene scan.
- `rm -rf .openmao && make demo && make demo-approve && rm -rf .openmao` passed.
- `rm -rf .openmao && npm run cli -- worker demo && npm run cli -- approvals approve approval_bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb && rm -rf .openmao` passed.

The default demo and the external-worker demo require no live external credentials or hosted
services.

### Optional gated live smoke (not part of `make check`)

With a fine-grained GitHub token that has issue/PR write permission on a throwaway repository:

```bash
OPENMAO_GITHUB_ENABLED=1 OPENMAO_CRED_GITHUB=<token> \
  # drive a governed github.create_issue_comment capability through approval and resume
```

The comment is created at most once, and the token never appears in `events`, `traces`,
`capability-calls`, `capability-results`, or logs.

## Acceptance Map

| Check | Required behavior | Evidence |
| --- | --- | --- |
| Real provider | At least one real, side-effecting capability provider exists and produces a structured result. | `ts/src/capabilities/providers/github.ts`, `ts/tests/github_provider.test.ts` |
| Credential broker | A non-secret `cred_*` handle resolves to a secret inside provider code only; absent/empty/whitespace resolves to none. | `ts/src/security/credential-broker.ts`, `ts/tests/credential_broker.test.ts` |
| Capability-bound credentials | A call's credential handle must match the capability's declared bound handle; a worker cannot steer to another configured secret. | `ts/src/capabilities/registry.ts`, `ts/tests/governance.test.ts` |
| No secret leakage | The resolved token is sent only in the Authorization header and never reaches a call, result, event, trace, artifact, or log; the runtime guard rejects `ghp_`/`github_pat_`/`sk-`/`xox` material before persistence. | `ts/src/capabilities/providers/github.ts`, `ts/src/security/sensitive-material.ts`, `ts/tests/sensitive_material.test.ts`, `ts/tests/governance.test.ts` |
| Async gateway | Provider execution is awaited outside the database transaction. | `ts/src/capabilities/registry.ts`, `ts/tests/governance.test.ts` |
| At most once | The provider executes at most once per call; a concurrent duplicate joins the in-flight execution, and resume after a database reopen returns the persisted result without re-executing. | `ts/src/capabilities/registry.ts`, `ts/tests/governance.test.ts` |
| Enforced approval | A high-risk side-effecting call suspends for approval before any network request and resumes only after approval. | `ts/src/capabilities/registry.ts`, `ts/tests/governance.test.ts` |
| Side-effect gate | An intrinsically side-effecting provider is gated even if a capability is misregistered as non-side-effecting. | `ts/src/capabilities/providers.ts`, `ts/src/capabilities/registry.ts`, `ts/tests/governance.test.ts` |
| Opt-in | The real provider is registered only when `OPENMAO_GITHUB_ENABLED=1` and `OPENMAO_CRED_GITHUB` resolves; with no env the runtime is mock-only. | `ts/src/runtime/capabilities.ts`, `ts/tests/runtime_capabilities.test.ts`, `.env.example` |
| Input safety | GitHub `owner`/`repo` are validated (no `.`/`..`), path segments are encoded, and the comment body is length-capped. | `ts/src/capabilities/providers/github.ts`, `ts/tests/github_provider.test.ts` |
| Portable contract | `Capability.credential_handle` carries its `cred_*` constraint in the generated JSON Schema and the TypeScript contract. | `ts/src/contracts/models.ts`, `schemas/canonical/v0.schema.json`, `ts/tests/contracts.test.ts` |
| Existing release paths | The local demo, learning loop, and external-worker gateway remain compatible. | smoke commands above, full `ts/tests` suite |

## At-most-once semantics

OpenMAO guarantees at most one provider invocation per capability call. It does not guarantee remote
exactly-once: a request that times out after GitHub created the comment but before OpenMAO records
success is recorded as failed even though the comment exists. Operators reconcile such cases by
inspecting the issue's comments before retrying rather than re-issuing blindly.

## Upgrade note

This release adds `Capability.credential_handle` and enforces capability-bound credentials. A local
`.openmao` database created before v0.4.0 holding a credential-requiring capability without the new
field will block such calls; reset local state (`rm -rf .openmao`) or re-register the capability when
upgrading. The default demos already reset local state.

## Release Gate Status

| Gate | Status |
| --- | --- |
| Implementation checks (`make check`) | Passing |
| Local demo compatibility smoke | Passing |
| External-worker gateway smoke | Passing |
| Public hygiene | Passing |
| Independent QA review | Required before stable promotion |
| Independent security review (live network + credential handling) | Required before stable promotion |
| Independent architecture review | Required before stable promotion |
| Maintainer release approval | Pending |

Because this release introduces live network mutation and credential handling, independent QA,
security, and architecture review are required before any stable promotion. Stable promotion remains a
separate maintainer decision after that review and any public feedback.
